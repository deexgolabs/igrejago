Django>=5.2,<5.3
python-dotenv>=1.0
django-htmx>=1.19
Pillow>=10.0
pandas>=2.2
openpyxl>=3.1
python-dateutil>=2.9
qrcode>=8.0
requests>=2.31
whitenoise>=6.7
reportlab>=4.0
icalendar>=6.0
pytest>=8.0
pytest-django>=4.8

# Produção
# psycopg2-binary>=2.9   # só necessário se DB_ENGINE=postgresql no .env
# gunicorn>=22.0
sentry-sdk>=2.0          # ativado — SENTRY_DSN no .env de produção
pywebpush>=2.0           # ativado — VAPID_* no .env de produção
